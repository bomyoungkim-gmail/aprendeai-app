{
  "name": "webclip-aprendeai-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Browser extension to capture web content and create WebClips in AprendeAI",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.6.0"
  }
}
